#include <iostream>

int main() {
    int userNumber;
    
    std::cout << "Enter an integer: ";
    std::cin >> userNumber;

    for (int i = 0; i <= 5 && i <= userNumber; ++i) {
        std::cout << i << " ";
    }

    return 0;
}
